---
title: Legend
---

import Example from '@site/src/components/Example'

# Legend

A hotkey legend bar that displays keyboard shortcuts. Commonly placed at the
bottom of a screen to show available actions.

Use `Legend` when you want to provide the items yourself. Use `AutoLegend`
when you want the legend to reflect the currently focused component's
`legendItems()` output and any labeled `HotKey` components.

Keys can be specified as plain strings (`'q'`, `'?'`) or named keys that are
automatically mapped to sigils (`'enter'` → `⤦`, `'escape'` → `␛`, etc.).
Modifier+key combinations can be expressed as arrays (`['ctrl', 'C']`) or
strings (`'Ctrl+C'`).

## Legend

<Example name="legend" title="Legend" />

`Legend` also supports a custom `separator` prop when you want a delimiter other
than the default double-space.

## AutoLegend

`AutoLegend` extends `Legend` and updates automatically as focus changes.

<Example name="auto-legend" title="AutoLegend" />

## Props

### Legend

| Prop        | Type           | Default | Description              |
| ----------- | -------------- | ------- | ------------------------ |
| `items`     | `LegendItem[]` | `[]`    | Array of key/label pairs |
| `separator` | `string`       | `'  '`  | Delimiter between items  |

### AutoLegend

| Prop        | Type     | Default | Description                            |
| ----------- | -------- | ------- | -------------------------------------- |
| `separator` | `string` | `'  '`  | Delimiter between auto-generated items |

### LegendItem

| Field   | Type                 | Description                                  |
| ------- | -------------------- | -------------------------------------------- |
| `key`   | `string \| string[]` | Key name(s) — mapped to sigils automatically |
| `label` | `string`             | Description shown after the key              |

## Key Sigil Reference

| Input                          | Rendered |
| ------------------------------ | -------- |
| `'enter'` / `'return'`         | `⤦`      |
| `'up'`                         | `↑`      |
| `'down'`                       | `↓`      |
| `'left'`                       | `←`      |
| `'right'`                      | `→`      |
| `'cmd'` / `'command'`          | `⌘`      |
| `'ctrl'` / `'control'`         | `⌃`      |
| `'alt'` / `'option'` / `'opt'` | `⌥`      |
| `'shift'`                      | `⇧`      |
| `'escape'` / `'esc'`           | `␛`      |
| `'tab'`                        | `⇥`      |
| `'space'`                      | `␣`      |
| `'backspace'`                  | `⌫`      |
| `'delete'`                     | `⌦`      |
| `'home'`                       | `↖`      |
| `'end'`                        | `↘`      |
| `'pageup'`                     | `⇞`      |
| `'pagedown'`                   | `⇟`      |

Plain strings (e.g. `'q'`, `'?'`, `'1-9'`) are rendered as-is.

## Wrapping

When items don't fit on a single line, Legend automatically wraps to multiple
rows with column-aligned keys and labels.
