---
sidebar_position: 5
title: Tabs
---

import TerminalScreenshot from '@site/src/components/TerminalScreenshot'

# Tabs

Tabbed container with clickable tab headers. Switch between views.

<TerminalScreenshot name="tabs" title="Tabs" />

## React Example

```tsx
import React from 'react'
import {Tabs, Text, run} from '@teaui/react'

function App() {
  return (
    <Tabs border>
      <Tabs.Section title="Info">
        <Text>Information panel content</Text>
      </Tabs.Section>
      <Tabs.Section title="Settings">
        <Text>Settings panel content</Text>
      </Tabs.Section>
      <Tabs.Section title="Help">
        <Text>Help panel content</Text>
      </Tabs.Section>
    </Tabs>
  )
}

run(<App />)
```

## Props

### Tabs

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `border` | `boolean` | `true` | Draw border around content |
| `children` | `Tabs.Section[]` | — | Tab sections |

### Tabs.Section

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `title` | `string` | — | Tab header text |
| `children` | `ReactNode` | — | Tab content |

<details>
<summary>Core API</summary>

```js
import {Tabs, Text} from '@teaui/core'

Tabs.create([
  ['Info', new Text({text: 'Information'})],
  ['Settings', new Text({text: 'Settings'})],
  ['Help', new Text({text: 'Help'})],
], {border: true})
```

</details>
