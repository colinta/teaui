{
  "name": "demos",
  "main": ".dist/index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @teaui/term --filter @teaui/core build && pnpm tsc",
    "checksum": "pnpm check --needs=../../packages/core/.dist --needs=../../packages/term/.dist . ../../packages/core/lib ../../packages/term/src 'pnpm build'",
    "demo": "node run.mjs",
    "clean": "rm -rf .dist/"
  },
  "private": true,
  "dependencies": {
    "@teaui/core": "workspace:*"
  },
  "devDependencies": {
    "@teaui/shared": "workspace:*"
  }
}
