{
  "name": "demos",
  "main": ".dist/index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @teaui/term --filter @teaui/core --filter @teaui/subprocess build && pnpm tsc",
    "checksum": "pnpm check 'pnpm build'",
    "demo": "node run.mjs",
    "clean": "rm -rf .dist/"
  },
  "private": true,
  "dependencies": {
    "@teaui/core": "workspace:*",
    "@teaui/subprocess": "workspace:*"
  },
  "devDependencies": {
    "@teaui/shared": "workspace:*"
  }
}
