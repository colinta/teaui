{
  "name": "demos",
  "main": ".dist/index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @teaui/term --filter @teaui/core --filter @teaui/react --filter @teaui/preact --filter @teaui/subprocess build && pnpm tsc",
    "checksum": "pnpm check",
    "demo": "node run.mjs",
    "clean": "rm -rf .dist/"
  },
  "private": true,
  "dependencies": {
    "@teaui/core": "workspace:*",
    "@teaui/subprocess": "workspace:*"
  },
  "devDependencies": {
    "@teaui/shared": "workspace:*"
  }
}
